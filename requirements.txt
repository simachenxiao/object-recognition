# 直接依赖（本项目实际 import 的包）
# 完整锁定版本见 requirements-lock.txt
#
# 安装：pip install -r requirements.txt

# ── 核心：识别模型（Chinese-CLIP）────────────────
torch==2.14.0                 # CPU 版实际为 2.14.0+cpu
torchvision==0.29.0
transformers==5.17.0          # ★ Chinese-CLIP 靠它加载（含中文 BERT 文本塔）

# ── 图像处理 ────────────────────────────────────
pillow==12.3.0

# ── 预留：后续 L0 层用（当前代码未 import）───────
opencv-python==5.0.0.93       # L0 差分 / 连通域
rapidocr-onnxruntime==1.2.3   # L0 证件 OCR
onnxruntime==1.30.0           # ONNX 推理
